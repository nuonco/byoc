name         = "admin_dashboard_alb_enabled"
description  = "Enable the ALB for the Nuon admin dashboard. When true, the admin dashboard service will be deployed with an ingress/ALB on admin.<root_domain>."
default      = "false"
display_name = "Admin Dashboard ALB Enabled"
required     = false
group        = "nuon"
type         = "bool"

user_configurable = false
