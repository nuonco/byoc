name        = "inspect_stack"
description = "Inspect install stack — AWS account, region, VPC, and full install_stack outputs."
readme      = "./runbooks/inspect_stack.md"
labels      = { service = "aws", type = "debug" }

[[steps]]
name        = "Inspect: installs"
type        = "action"
action_name = "inspect_installs"
