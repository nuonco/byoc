# S3 Buckets

Creates three buckets:

1. `clickhouse` bucket for backups and s3 disk
2. `install_templates` for CloudFormation templates for stacks and custom nested stacks.
3. `blob_storage` for custom GORM blob storage type.
