type           = "deprovision"
name           = "{{.nuon.install.id}}-deprovision"
description    = "Deprovision Role for BYOC Nuon"
display_name   = "deprovision"
cloud_platform = "gcp"

# GCP equivalent of AdministratorAccess. TODO: enumerate the full destruction set as scoped
# grants (mirroring the AWS deprovision-base policy) before dropping owner.
[[policies]]
name                = "owner"
gcp_predefined_role = "roles/owner"
