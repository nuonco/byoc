#:schema https://api.nuon.co/v1/general/config-schema?type=action
# action
name    = "ctl_api_query_builds"
labels  = { service = "ctl-api", type = "report" }
timeout = "5m"

[[triggers]]
type          = "cron"
cron_schedule = "*/10 * * * *"

[[triggers]]
type = "manual"

[[steps]]
name            = "Query recent component builds"
inline_contents = "./ctl_api/ctl_api_query_builds/query-builds.sh"

[steps.env_vars]
# The component_builds table (and the other ctl-api tables) are owned by the
# ctl-api IAM database role, so we must query as that role — the Cloud SQL admin
# user (nuon-db) gets "permission denied". DB_USER is the IAM DB user;
# CTL_API_SA_EMAIL is the SA the runner impersonates to mint a Cloud SQL login
# token (Cloud SQL IAM auth).
DB_USER          = "{{ .nuon.components.ctl_api_wi.outputs.db_user }}"
CTL_API_SA_EMAIL = "{{ .nuon.components.ctl_api_wi.outputs.service_account_email }}"
DB_NAME          = "ctl_api"
DB_ADDR          = "{{ .nuon.components.cloudsql_nuon.outputs.address }}"
DB_PORT          = "5432"
LIMIT            = "25"

[[steps]]
name            = "timestamp"
inline_contents = "./ctl_api/ctl_api_query_builds/timestamp.sh"
