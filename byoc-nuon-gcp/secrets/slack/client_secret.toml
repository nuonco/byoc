name         = "slack_client_secret"
display_name = "Slack Client Secret"
description  = "Client Secret from your Slack app's Basic Information page. Managed out-of-band: the central GCP Secret Manager entry referenced by slack_secrets_name is the source of truth, and the sync_slack_secrets action writes the value into k8s."
default      = "dne" # "empty" value — disables Slack integration

user_configurable = true
