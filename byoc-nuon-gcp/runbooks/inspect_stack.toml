name        = "inspect_stack"
description = "Inspect install stack — GCP project, region, network, and full install_stack outputs."
readme      = "./runbooks/inspect_stack.md"
labels      = { service = "gcp", type = "debug" }

[[steps]]
name        = "Inspect: installs"
type        = "action"
action_name = "inspect_installs"
