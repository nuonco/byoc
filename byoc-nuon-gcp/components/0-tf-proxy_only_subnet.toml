#:schema https://api.nuon.co/v1/general/config-schema?type=terraform
name              = "proxy_only_subnet"
type              = "terraform_module"
terraform_version = "1.11.3"

[public_repo]
repo      = "nuonco/byoc"
directory = "byoc-nuon-gcp/src/components/proxy_only_subnet"
branch    = "main"

[vars]
install_id = "{{ .nuon.install.id }}"
project_id = "{{ .nuon.install_stack.outputs.project_id }}"
region     = "{{ .nuon.install_stack.outputs.region }}"
network    = "{{ .nuon.sandbox.outputs.vpc.network_self_link }}"
