name         = "nuon_access_secret_arn"
description  = "DEPRECATED and no longer read by anything. Nuon Access is now configured with the ctl_api_add_identity_provider action. Retained so existing installs that set it keep syncing; safe to remove once every install has cleared it."
default      = ""
display_name = "Nuon Access Secrets ARN"
required     = false
group        = "nuon"

user_configurable = false
