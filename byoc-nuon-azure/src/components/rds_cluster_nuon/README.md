# Azure PostgreSQL Flexible Server (Nuon)

This component provisions a PostgreSQL Flexible Server and database on Azure for the Nuon control plane.

The implementation is in `main.tf`, with outputs in `outputs.tf`.
