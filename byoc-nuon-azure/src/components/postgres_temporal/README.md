# Azure PostgreSQL Flexible Server (Temporal)

This component provisions a PostgreSQL Flexible Server and database on Azure for Temporal.

The implementation is in `main.tf`, with outputs in `outputs.tf`.
