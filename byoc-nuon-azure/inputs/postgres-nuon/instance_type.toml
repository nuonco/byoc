#:schema http://localhost:8081/v1/general/config-schema?type=inputs
name         = "nuon_db_instance_type"
description  = "What Azure PostgreSQL Flexible Server SKU should be used for the Nuon DB?"
default      = "GP_Standard_D4ds_v5"
display_name = "PostgreSQL SKU"
group = "postgres-nuon"
internal     = true

user_configurable = false
