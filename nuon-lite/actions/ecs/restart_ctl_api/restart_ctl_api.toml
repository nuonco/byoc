# Force-new-deployment on all three ctl-api services.
name    = "restart_ctl_api"
labels  = { service = "ctl-api", type = "ops" }
timeout = "5m"

[[triggers]]
type = "manual"

[[steps]]
name            = "restart ctl-api services"
inline_contents = "./ecs/restart_ctl_api/restart.sh"

[steps.env_vars]
CLUSTER  = "{{ .nuon.components.ecs_runtime.outputs.cluster_name }}"
SERVICES = "n-{{ .nuon.install.id }}-ctl-api-public n-{{ .nuon.install.id }}-ctl-api-admin"
REGION   = "{{ .nuon.install_stack.outputs.region }}"
