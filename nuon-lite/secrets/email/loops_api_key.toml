name          = "loops_api_key"
display_name  = "Loops API Key"
description   = "API key from Loops for transactional email (invites, password resets). Optional — ctl-api will skip outbound email if absent."
auto_generate = false
