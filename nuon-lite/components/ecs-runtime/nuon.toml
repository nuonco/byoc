# terraform
name              = "ecs_runtime"
type              = "terraform_module"
terraform_version = "1.13.5"
max_auto_retries  = 5

[public_repo]
repo      = "nuonco/byoc"
directory = "nuon-lite/components/ecs-runtime"
branch    = "ja/aws-ecs"

[vars]
region  = "{{ .nuon.install_stack.outputs.region }}"
vpc_id  = "{{ .nuon.install_stack.outputs.vpc_id }}"
nuon_id = "{{ .nuon.install.id }}"
